# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 1.0.x | ✅ 受支持 |

## 报告漏洞

如果你发现了安全漏洞，请通过私下渠道（Issue 邮件/私信维护者）报告，
**不要在公开 Issue 中披露细节**。我们会在确认后尽快修复并发布补丁。

请勿提交 `.jks` / `.keystore` / 密钥等敏感文件到仓库（已在 `.gitignore` 中排除）。

## 已知安全注意事项

- 本框架为客户端游戏逻辑，请勿将关键计分、掉落概率等逻辑完全依赖客户端，
  联网对战时应在服务端做权威校验。
- 事件广播 `GlobalEventCenter.Raise` 参数为 `object`，订阅方应自行做类型检查。
